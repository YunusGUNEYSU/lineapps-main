import 'package:flutter/material.dart';

final class AppPading {
  AppPading._();
  static const normalPadding = EdgeInsets.all(8);
  static const horizontalNormalPadding = EdgeInsets.symmetric(horizontal: 8);
  static const verticalNormalPadding = EdgeInsets.symmetric(vertical: 8);
  static const lowPadding = EdgeInsets.all(4);
  static const horizontalLowPadding = EdgeInsets.symmetric(horizontal: 4);
  static const verticalLowPadding = EdgeInsets.symmetric(vertical: 4);
  static const highPadding = EdgeInsets.all(12);
  static const horizontalHighPadding = EdgeInsets.symmetric(horizontal: 12);
  static const verticalHighPadding = EdgeInsets.symmetric(vertical: 12);
  static const veryhighPadding = EdgeInsets.all(15);
  static const horizontalVeryHighPadding = EdgeInsets.symmetric(horizontal: 15);
  static const verticalVeryHighPadding = EdgeInsets.symmetric(vertical: 15);
}
